{
  "name": "workout-manager",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @workout-manager/api dev",
    "build": "pnpm -r build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "db:generate": "pnpm --filter @workout-manager/db db:generate",
    "db:migrate": "pnpm --filter @workout-manager/db db:migrate",
    "db:seed": "pnpm --filter @workout-manager/db db:seed"
  },
  "packageManager": "pnpm@9.15.0"
}
